// Firebase Storage 규칙 (firebase.rules)
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 모든 인증된 사용자에게 읽기/쓰기 권한 (개발용)
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
